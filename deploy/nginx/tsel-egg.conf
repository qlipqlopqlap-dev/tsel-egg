# nginx reverse proxy for Pecahkan Telur (PM2: tsel-egg @ :5282).
# Install: copy to /etc/nginx/sites-available/tsel-egg.conf,
#          ln -s ../sites-available/tsel-egg.conf /etc/nginx/sites-enabled/,
#          nginx -t && systemctl reload nginx
#
# Serves one app on :5282. server.mjs serves index.html for any non-file path,
# so nginx needs no try_files.

upstream tsel_egg {
    server 127.0.0.1:5282;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;

    server_name telkomsel3.qlipmobile.com;

    gzip on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml application/manifest+json;

    # Content-hashed assets → cache hard at the edge/browser.
    location /assets/ {
        proxy_pass http://tsel_egg;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # App. server.mjs handles SPA fallback.
    location / {
        proxy_pass http://tsel_egg;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";
        proxy_read_timeout 60s;
    }
}

# ── HTTPS ──────────────────────────────────────────────────────────────────
# Option A — certbot:  sudo certbot --nginx -d telkomsel3.qlipmobile.com
# Option B — Cloudflare proxied, SSL/TLS mode "Full".
